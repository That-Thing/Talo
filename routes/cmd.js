var express = require('express');
var router = express.Router();
const config = require('../modules/config');

//Commands that require backend processing will be handled here isntead of clientside JS.

module.exports = router;
